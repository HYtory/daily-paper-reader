---
title: Nonlinear Guidance for Arrival Time and Arrival Angle Control Using Trajectory Shaping
title_zh: 基于轨迹成形的到达时间和到达角度非线性制导
authors: "Kun Wang, Andrés Infante Adrián"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21643v1"
tags: ["query:ui"]
score: 9.0
evidence: 到达时间和角度控制的非线性制导方法直接支持单无人机拦截任务
tldr: 针对恒速固定翼无人机的到达时间和角度控制问题，提出基于四阶多项式轨迹成形的非线性制导方法。将制导问题转化为求解两个耦合非线性积分方程，通过两阶段求解策略：先构造解析热启动获得初始猜测，再精化并求解原方程组。仿真表明，该方法在强非线性场景下得到的解接近开环最优，优于部分现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21643v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 798, \"height\": 458, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21643v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1698, \"height\": 1091, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21643v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 709, \"height\": 1437, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21643v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1701, \"height\": 968, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21643v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 710, \"height\": 1422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21643v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 635, \"height\": 273, \"label\": \"Table\"}]"
motivation: 现有到达时间角度控制制导方法在高非线性场景下难以生成可行解。
method: 将视线角参数化为四阶多项式，通过两阶段求解耦合积分方程：先解析近似获得初始参数，再精化并求解原二维方程组。
result: 在测试案例中，所提方法得到的解非常接近开环最优解，即使在高非线性场景下也能稳定生成可行解。
conclusion: 提出的非线性轨迹成形制导策略有效解决了强非线性耦合问题，具有接近最优的性能和更强的适用性。
---

## 摘要
本文针对恒速固定翼无人机的到达时间和角度控制，提出了一种非线性轨迹成形制导策略。将视角参数化为四阶多项式。非线性制导问题转化为求解两个耦合的非线性积分方程，其中包含两个未知制导参数。直接求解这些方程由于参数间的强耦合而具有挑战性。为此，开发了一种两阶段求解方法。在第一阶段，构建了解析热启动。具体地，通过应用近似，获得了两个制导参数之间的线性关系。利用这一关系，推导出第一个制导参数的标量二次方程，从而得到第一个制导参数的良好初始猜测。在第二阶段，通过求解一维非线性方程来细化该初始猜测。最终将得到的解用作求解原始二维非线性方程组的初始猜测。数值模拟表明，对于测试案例，所得解非常接近开环最优解，即使在高度非线性场景中，某些现有方法无法生成可行解的情况下也是如此。

## Abstract
This paper proposes a nonlinear trajectory shaping guidance strategy for arrival time and angle control of a constant-speed fixed-wing unmanned aerial vehicle. The look angle is parameterized by a fourth-order polynomial. The nonlinear guidance problem is transformed into solving two coupled nonlinear integral equations with respect to two unknown guidance parameters. Directly solving these equations is challenging due to the strong coupling between the parameters. To address this, a two-stage solution procedure is developed. In the first stage, an analytical warm start is constructed. Specifically, by applying approximations, a linear relationship between the two guidance parameters is obtained. With this relationship, a scalar quadratic equation in the first guidance parameter is derived, leading to a good initial guess for the first guidance parameter. In the second stage, this initial guess is refined by solving a one-dimensional nonlinear equation. The obtained solution is finally used as the initial guess for solving the original two-dimensional nonlinear system of equations. Numerical simulations demonstrate that for the test cases, the obtained solution is very close to the open-loop optimal solution, even in highly nonlinear scenarios where some existing methods fail to generate a feasible solution.

---

## 论文详细总结（自动生成）

# 论文详细总结：基于轨迹成形的到达时间和到达角度非线性制导

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：无人机自主任务中，除引导飞行器到达目标点外，常需同时满足到达时间（时敏任务、多机协同）和到达角度（提高末段性能）的精确控制。现有方法分为线性制导（依赖时变估计，高非线性场景易失效）和非线性制导（如滑模控制、非线性最优控制、轨迹成形等）。然而，轨迹成形方法虽无需线性化，但求解产生的非线性方程组存在强耦合、收敛困难的问题，尤其在高度非线性场景中可能无法生成可行解。
- **整体含义**：本文提出一种基于四阶多项式视角参数化的非线性轨迹成形制导律，通过两阶段求解策略，实现到达时间和角度的联合控制，使解接近开环最优解，并能在强非线性场景下稳定工作。

## 2. 方法论：核心思想、关键技术细节、算法流程

- **核心思想**：将无人机的视角（视场角）参数化为关于时间的四阶多项式，利用多项式结构自动满足末端视角为零和一阶导数为零的条件，从而将非线性制导问题转化为求解两个未知参数 (κ₁, κ₂) 的耦合非线性积分方程组。为克服强耦合带来的求解困难，设计两阶段求解过程。
- **关键技术细节**：
    - **视角参数化**：
        σ(t) = (t - 1)² (σ₀ + κ₁ t + κ₂ t²)，其中 σ₀ 为初始视角，κ₁、κ₂ 为待定制导参数。该形式自然满足 σ(1)=0 和末段加速度为零。
    - **问题转化**：
        制导问题转化为求解 F(κ₁, κ₂) = [ r₀ - ∫ cosσ dt, λ₀ - γ_f - ∫ (sinσ / r) dt ]ᵀ = 0。
    - **两阶段求解策略**：
        - **第一阶段：解析热启动**
            1. 采用小角度近似和近似线性距离消耗假设，推导出 κ₂ = Γ - 2κ₁ 的线性关系（Γ 由初始条件和期望角度计算）。
            2. 利用余弦二次近似将距离积分方程转化为关于 κ₁ 的标量二次方程，求解获得两个候选解；若判别式非正，取顶点值。
            3. 通过近似控制能量 Ĵ 选择较小能量候选，得到初始猜测 κ₁^Taylor。
        - **第二阶段：精化与原始方程组求解**
            4. 求解一维非线性方程 r₀ - ∫ cos(σ(κ₁, Γ-2κ₁)) dt = 0，获得精化后的 κ₁⁰，再还原 κ₂⁰。
            5. 将 [κ₁⁰, κ₂⁰] 作为初始猜测，使用 MATLAB fsolve 求解原始二维非线性方程组 (6)，得到最终制导参数。
    - **算法流程**：Algorithm 1 详细描述了该两阶段过程，包括判别式判断、候选选择、一维方程求解和最终二维求解。

## 3. 实验设计

- **场景与数据集**：均采用仿真场景，使用归一化时间（t_f=1）再缩放回物理单位。包含：
    - **闭环实现测试**：初始条件 r₀=5000 m, λ₀=0°, γ₀=30°, γ_f=-60°, t_f=35 s, V=200 m/s。制导更新间隔 0.01 s，末段切换为比例制导。
    - **对比案例 A**（源于文献[4]）：r₀=15000 m, λ₀=90°, γ₀=0°, γ_f=-60°, t_f=90 s, V=250 m/s。
    - **对比案例 B（强非线性）**：r₀=10000 m, λ₀=0°, γ₀=120°, γ_f=120°, t_f=150 s, V=200 m/s。
    - **多机编队应用**：三架 UAV 分别前往等边三角形顶点，共享 t_f=55 s, γ_f=0°，各自初始位置和速度不同。
- **Benchmark**：以 GPOPS-II 生成的开环最优解作为基准。
- **对比方法**：
    1. 文献[14]轨迹成形方法（等价于本文四阶多项式，但缺乏有效初始化）。
    2. Hou 等人[4]的几何虚拟目标制导律。
    3. GPOPS-II 开环最优求解器（基准）。
- **评价指标**：控制能量 J = ∫ a²(t) dt；轨迹、角度、加速度的对比；收敛迭代次数。

## 4. 资源与算力

- 文中**未明确说明**所使用的算力资源（如 GPU 型号、数量、训练时长等）。求解使用 MATLAB 的 fsolve 函数，在普通计算机上即可运行，作者强调计算效率高（闭环中首次迭代最多10次，之后通常1次或不需迭代），但未给出硬件配置。

## 5. 实验数量与充分性

- **实验数量**：主要包含三组独立测试：闭环实现（单场景）、对比案例 A、对比案例 B，以及一个多机编队应用案例。未进行消融实验或参数敏感性分析。
- **充分性与公平性**：
    - 对比案例 A 中，本文方法、Hou 法、GPOPS-II 均成功，本文控制能量最接近基准；案例 B（强非线性下）只有本文方法和 GPOPS-II 成功，充分展示了方法的鲁棒性。
    - 但实验场景数量较少（仅两个对比案例和一个应用例），未覆盖随机初始条件或不同速度/约束组合的大规模蒙特卡洛测试。对加速度饱和、扰动等鲁棒性未显式分析（作者指出可通过高更新率闭环处理，但未提供定量验证）。总体而言，实验能支撑核心论点，但充分性一般。

## 6. 主要结论与发现

- 所提两阶段求解策略能高效稳定地求解非线性耦合方程组，生成可行解。
- 所得解在控制能量 J 上非常接近 GPOPS-II 开环最优解（案例 A 误差 0.05%，案例 B 误差约 1.4%），且明显优于 Hou 等人的几何法。
- 在强非线性场景（大初始/终末航向角、长飞行时间）中，现有方法（文献[14]和 Hou 法）失败，而本文方法仍能成功生成轨迹。
- 闭环实现中首次迭代最多 10 次，后续迭代极少，具备实时计算潜力。
- 多机编队应用展示了该制导律在多 UAV 协同中的有效性，末端加速度为零有利于保持编队。

## 7. 优点

- **方法创新性强**：将视角四阶多项式参数化与两阶段求解策略结合，有效解决强耦合非线性方程组求解难题。
- **性能接近最优**：无需求解复杂最优控制问题，即可获得接近开环最优的解，计算效率高。
- **鲁棒性突出**：在高度非线性场景下明显优于现有方法，尤其文献[14]的失败对比凸显了初始化策略的重要性。
- **设计简洁**：解析热启动基于小角度和线性距离假设，但最终仍精确满足终端约束，平衡了近似精度与求解收敛性。
- **适用场景丰富**：可应用于多机编队、协同到达等任务，并可直接处理时间与角度联合约束。

## 8. 不足与局限

- **实验覆盖有限**：仅两个对比案例（一个常规、一个极端）和一个应用例，缺乏对参数空间（初始距离、角度组合、不同期望时间等）的系统扫描或统计验证。
- **未考虑实际扰动与不确定性**：作者声明未显式分析风扰、传感器噪声等的影响，仅依赖高更新率闭环处理，但未提供闭环鲁棒性定量指标（如误差范围、更新率要求）。
- **理论假设局限**：小角度和线性距离近似仅在热启动阶段使用，但配方本身要求视角多项式形式，可能不适用于必须严格避免大机动或视场角超出约束的场景（如本文未分析视场角约束，虽有文献[21]提及但未在本工作中处理）。
- **未提供收敛性分析**：尽管实验显示收敛，但未给出数学上保证收敛的条件（如初始猜测与真实解的距离范围、雅可比矩阵非奇异等）。
- **未对比计算时间与平台要求**：虽然提及迭代次数少，但未与对比方法（Hou 法、GPOPS-II）进行定量时间对比，也未说明实时实现所需的最小更新率。

（完）
