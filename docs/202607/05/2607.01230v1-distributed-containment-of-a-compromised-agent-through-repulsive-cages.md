---
title: Distributed Containment of a Compromised Agent through Repulsive Cages
title_zh: 通过排斥笼对受损智能体的分布式遏制
authors: "Luigi Petruzziello, Camilla Fioravanti, Gabriele Oliva"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01230v1"
tags: ["query:ui"]
score: 8.0
evidence: 利用排斥笼分布式围堵被攻陷智能体
tldr: 针对无人机集群等安全关键系统中合法智能体被劫持的安全风险，提出分布式围堵框架。利用低级碰撞避免模块在被劫持下仍活跃的特性，通过防御者配置几何形状形成“排斥笼”间接操控目标运动。将交互建模为在线Stackelberg博弈，给出单步围堵的精确几何特征，并设计分布式近似算法。理论证明次线性动态遗憾界，仿真验证有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01230v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 657, \"height\": 370, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01230v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 865, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01230v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 868, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01230v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 868, \"height\": 521, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01230v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 867, \"height\": 523, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01230v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1772, \"height\": 497, \"label\": \"Figure\"}]"
motivation: 现有方法侧重检测隔离恶意智能体，但未利用低级安全模块的防御潜力，需开发利用该特性的分布式围堵机制。
method: 基于Stackelberg博弈，防御者为领导者，通过几何配置形成排斥笼间接约束目标；推导基于支撑函数和法锥的精确几何特征，设计分布式在线近似算法。
result: 证明了相对于集中式基准的次线性动态遗憾界，量化了网络估计误差的影响，仿真验证围堵性能。
conclusion: 提出利用碰撞避免模块的分布式围堵框架，提供理论保障，适用于被劫持智能体的安全约束与引导。
---

## 摘要
无人机群和网络物理多智能体系统越来越多地部署在需要协调运动、分布式决策和自主性的安全关键任务中。当一个合法智能体被劫持并受到对抗性高层命令驱动时，会产生重大安全风险。我们并不专注于恶意智能体的检测和隔离，而是利用自主平台中常见的结构特性：低级避碰模块通常作为独立安全层实现，即使在高层被攻破时也可能保持激活。基于这一特性，我们提出了一种分布式遏制框架，利用受损智能体未受损的避碰响应作为间接驱动通道。防御智能体选择其几何构型来塑造目标所经历的排斥场，目标是将其限制在规定的允许区域内，并在需要时将其引导至期望目的地。该交互被建模为一个在线斯塔克尔伯格博弈，其中防御者作为领导者，对手通过选择目标命令进行反应。利用支撑函数和法锥论据，我们推导了鲁棒单步遏制的精确几何特征，并引入了排斥笼的概念。这些结果定义了一个集中式斯塔克尔伯格预言机，并激发了一种基于本地通信和动态场估计的完全分布式在线近似。我们证明了相对于集中式基准的次线性动态遗憾界，量化了网络引起的估计误差和阶段最优值的时间变异性的影响。仿真验证了该方法并支持了理论。

## Abstract
UAV swarms and cyber-physical multi-agent systems are increasingly deployed in safety-critical missions that require coordinated motion, distributed decision making, and autonomy. A major security risk arises when a legitimate agent is hijacked and driven by adversarial high-level commands. Rather than focusing on detection and isolation of malicious agents, we exploit a structural property common in autonomous platforms: low-level collision-avoidance modules are typically implemented as independent safety layers and may remain active even under high-level compromise. Building on this property, we propose a distributed containment framework that uses the compromised agent's uncompromised avoidance response as an indirect actuation channel. Defender agents select their geometric configuration to shape the repulsive field experienced by the target, with the goal of keeping it inside a prescribed admissible region and, when required, steering it toward a desired destination. The interaction is modeled as an online Stackelberg game in which defenders act as leaders and the adversary reacts by choosing the target command. Using support-function and normal-cone arguments, we derive an exact geometric characterization of robust one-step containment and introduce the notion of a repulsive cage. These results define a centralized Stackelberg oracle and motivate a fully distributed online approximation based on local communication and dynamic field estimation. We prove sublinear dynamic-regret bounds with respect to the centralized benchmark, quantifying the effect of network-induced estimation errors and temporal variability of the stage-wise optimum. Simulations validate the approach and corroborate the theory.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **安全风险**：在无人机集群（UAV swarms）和网络-物理多智能体系统中，合法智能体可能被外部攻击者劫持，导致其高层命令通道受控，从而威胁整个任务的安全性与协作。
- **现有方法的局限**：传统安全策略主要聚焦于攻击检测、隔离或排除恶意节点，但未能利用自主平台中一个结构性的弱点：**低级碰撞避免模块**通常独立于高层命令层实现，即使高层被攻破，该安全层仍可能保持激活。  
- **本文思路**：避开检测和隔离，而是“以毒攻毒”——利用受损目标**自身仍生效的低级避碰响应**作为间接驱动通道。防御者（defenders）通过选择几何构型来塑造目标受到的排斥场，从而间接将其限制在允许区域（admissible region）内，甚至按需引导其向目的地移动。  
- **建模为在线博弈**：将每个时间步的交互建模为**在线斯塔克尔伯格（Stackelberg）博弈**，防御者作为领导者（leader），敌方作为追随者（follower）。该问题的核心是**分布式场塑形（field-shaping）**，而非直接命令目标。

### 2. 论文提出的方法论

- **核心思想**：利用两层控制架构的不对称性——防御者的高层命令经低级避碰层后产生“诱导配置”（induced configuration）；目标的低级避碰层响应于防御者的位置产生聚合排斥场；防御者通过选择诱导配置来间接控制目标。
- **关键技术细节**：
  - **单步鲁棒围堵的几何条件**：通过支撑函数（support function）和法锥（normal cone）论证，给出等价条件：  
    `Ψ_k(y) = 0 ⇔ c_k(y) ≤ 0`，其中 `c_k(y)` 定义为方向余量（directional containment margin）的上确界。  
    据此定义**排斥笼（Repulsive Cage）**：使目标所有可能的对抗命令下都留在允许区域的防御者配置。
  - **集中式斯塔克尔伯格预言机**：求解形如 `min_{y∈Y_k} F_k(y)` 的一步优化问题，其中损失函数包含违反惩罚、运输赤字惩罚和防御者侧正则项。存在性由 Weierstrass 定理保证。
  - **分布式在线近似**：
    - **动态平均一致性（dynamic average consensus）**：防御者追踪聚合场 `g_k(y)` 的局部估计（式 (65)），并证明共识偏差积累有界（定理 2）。
    - **主动方向选择**：利用共享的 δ-网络方向字典，通过有限轮分数一致性（score consensus）选出当前最危险方向 ν_k，以近似子梯度中的方向项。
    - **局部梯度更新**：每个防御者计算自己的扰动子梯度方向 `d_i[k]`，然后投影到可行集并映射回命令（式 (96)~(100)）。
- **算法流程（文字描述）**：
  1. 每个时间步 k，防御者 i 获得目标位置、本地状态和邻居信息。
  2. 运行动态平均共识更新自身聚合场估计 `s_i[k]`，并据此计算本地预测场 `ĝ_i[k]`.
  3. 通过内部分数共识确定公共方向 ν_k（使用字典或几何规则如投影方向）。
  4. 计算本地梯度方向 `d_i[k]`（包含子梯度项、运输项、正则项）。
  5. 以步长 η 执行投影梯度步得到 `y_i[k+1]`，再映射为可执行的命令 `u_i[k]`.
  6. 物理执行后更新本地避碰贡献 `r_i[k+1]`，为下一轮共识做准备。

### 3. 实验设计

- **仿真场景**：2D 环境，5 个防御者 + 1 个劫持目标；允许区域 Ω 为移动圆盘（半径 1.15），对抗命令集为圆盘（半径 0.07），防御者每步最大位移 0.20。目标初始位于允许区域中心，防御者初始分布在松散环状支撑上。
- **Benchmark**：**集中式斯塔克尔伯格预言机**（投影有限差分下降求解一期优化问题）。没有与其他间接放牧方法（如 StringNet）进行直接数值对比。
- **对比方法**：
  - **主动方向（active direction）**：通过分数一致性选择字典中最优方向。
  - **投影方向（projection direction）**：利用本地预测位置到允许区域的投影方向（更廉价但精度稍低）。
- **评估指标**：即时遗憾（instantaneous regret）、累积遗憾（cumulative regret）、平均遗憾、单步最坏违反量 Ψ_k.
- **随机种子**：所有仿真使用种子 11. 无额外数据集使用。

### 4. 资源与算力

- 论文中未提及 GPU 型号、数量或训练时长，也未提及训练过程（采用在线分布式投影梯度更新，无离线训练）。推测在普通 CPU 上运行 MATLAB 仿真。
- 资源开销很小，主要体现在动态平均共识和简单梯度计算。

### 5. 实验数量与充分性

- **实验数量**：仅展示单一场景下（n=5，固定参数）两个变种（主动方向/投影方向）的运行结果（图 2~6）。未进行**消融实验**（如不同防御者数量、不同允许区域形状、不同对抗集大小、不同步长、不同正则权重等）。也未与其他文献中的间接放牧方法做横向对比。
- **充分性**：实验覆盖非常有限，仅作为理论结果的直观验证。理论部分提供了严格的遗憾界（定理 3、推论 1），仿真证实了趋势（累积遗憾近似子线性、违反量很小），但缺乏在多变条件下的鲁棒性检验。因此实验证据的充分性和客观性不足。

### 6. 论文的主要结论与发现

- **理论结论**：在温和假设下，分布式在线算法相对于集中式基准的动态遗憾上界为 `O(√T + E_T)`，其中 `E_T` 包含跟踪误差、方向误差和实施误差。当路径变化量 P_T、场变化量 V_r^T、方向误差等均为 o(T) 时，平均遗憾趋于零（定理 3、推论 1）。
- **仿真结论**：两种分布式近似（主动方向和投影方向）均能跟踪集中式基准，累积遗憾增长缓慢（平均遗憾下降趋势），单步最坏违反量极低（约 10^{-5}，接近零），表明算法在所示场景下有效实现了鲁棒围堵。

### 7. 优点

- **方法新颖**：利用被劫持智能体**自身仍工作的低级安全层**作为间接控制通道，区别于主流的检测/隔离思路。
- **理论严谨**：给出单步鲁棒围堵的精确几何特征（支撑函数-法锥公式），并定义排斥笼。实现了从集中式最优到分布式在线更新的严格遗憾分析，包含网络效应和方向近似误差的量化。
- **完全分布式**：仅依赖本地度量、邻居通信和动态共识，无中心节点，适合大规模集群。
- **考虑在线与对抗性**：建模为在线 Stackelberg 博弈，无需先验知道对手策略，采用最坏-case 视角。

### 8. 不足与局限

- **实验覆盖不足**：仅用 5 个防御者、单一参数集、单一场景。缺乏不同规模、不同几何约束、不同对抗集强度、不同通信拓扑等条件下的消融研究。未与现有间接放牧方法（如 StringNet）进行数值对比，无法评判相对性能。
- **假设较强**：网络图必须连通、混合矩阵需满足双随机收缩条件（Assumption 1）、场函数的连续性和凸性（Assumption 4,5）在某些非凸情形下不成立。实际中低级避碰层可能非凸或非线性。
- **未考虑现实误差源**：未包含通信延迟、丢包、测量噪声、模型失配等。虽然部分可在误差项中吸收，但未作定量分析。
- **未涉及硬件验证**：仅限于仿真，缺乏真实无人机平台实验。
- **参数敏感性**：步长 η、正则权重 α、β 等对性能有影响，但未讨论自适应策略或鲁棒性测试。
- **通用性限制**：方案依赖于目标保持低级避碰功能；若攻击者能完全覆写整个控制栈，则本方法失效。
- **理论遗憾界中的常数**：可能较大，实际性能与理论保证间存在差距，需要更精细的实验校准。

（完）
